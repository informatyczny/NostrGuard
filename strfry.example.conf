relay {
    bind = "0.0.0.0"
    port = 8080

    info {
        name = "Some Events Node"
        description = "Curated mirror — registered volunteers only"
        contact = "contact@example.com"
    }

    writePolicy {
        plugin = "/app/scripts/relay_policy.py"
    }
}

db = "/app/strfry-db"
